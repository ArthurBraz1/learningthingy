#include <stdio.h>
#include <math.h>
int main(void){
    int number;
    
printf("Insert the number to find the module of\n");
scanf("%i", &number);
int numbermodule = sqrt(pow(number,2));
printf("The module is %i\n",numbermodule);



}